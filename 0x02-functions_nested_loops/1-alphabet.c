#include "main.h"
/**
 * main - to tell if a random number is either - or +
 *
 * n is the random integer
 *
 * Return: the integer
 */
int main(void)
{
char cha = 'a';

while (cha <= 'z')
{
	_putchar(cha);
	cha++;
}
_putchar('\n');
return (0);
}
