#include "main.h"

/**
 * add - returns the sum of two parameters
 * @a: first parameter
 * @b: second parameter
 *
 * Description: sums two numbers
 * Return: Always (0).
 */

int add(int a, int b)
{
return (a + b);
}

