#include "main.h"

/**
 * main - to tell if a random number is either - or +
 *
 * n is the random integer
 *
 * Return: the integer
 */
int main(void)
{
char *txt = "_putchar";
int lenths = 8;
int i = 0;

for (; i < lenths ; i++)
{
	_putchar( txt[i]);
}
_putchar('\n');
return (0);
}
