#include <stdlib.h>
#include <stdio.h>

/**
 * main - to tell if a random number is either - or +
 *
 * n is the random integer
 *
 * Return: the integer
 */
int main(void)
{
int i = 0;
char cha = 'a';

for (; i <= 9 ; i++)
{
	putchar(i);
}
for (; cha <= 'f' ; cha++)
{
	putchar(i);
}
printf("\n");
return (0)
}
