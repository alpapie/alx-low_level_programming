#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strcmp - returns the largest of 3 numbers
 * @s1: first integer
 * @s2: second integer
 * Return: n
 */

int _strcmp(char *s1, char *s2)
{
int result ;

result = strcmp(s1,s2)
return (result);
}
