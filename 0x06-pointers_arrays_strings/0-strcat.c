#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strcat - returns the largest of 3 numbers
 * @dest: first integer
 * @src: second integer
 * Return: char 
 */

char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
