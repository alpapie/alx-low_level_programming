#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the largest of 3 numbers
 * @s: first integer
 * Return:length of the string
 */

int _strlen(char *s)
{
return (strlen(s));
}
